# U-01 구현·검증 기록

Code Generation Part 2 구현·필수 검증 완료, 코드 산출물 승인 대기. 원본 수용조건은 [stories.md](../../../inception/user-stories/stories.md)를 참조한다. 실제 제품 검증은 아래에 별도로 기록한다. 단위/합성 검사를 실제 브라우저·모델 성공 증거로 사용하지 않는다.

## 현재 완료 상태와 수용조건 근거

| 기능 / AC | 구현 | 실제 확인 / 출처 | 남은 범위 |
|---|---|---|---|
| 도구·기본 Skill 생성 / AC-01·02·04 | 완료: generation, MCP, executor | 직접: Opus 생성 → 새 입력별 도구/Skill → 자기 탭 사후 상태 | 지원 범위는 합성 MinIO 표준 DOM |
| 채팅·준비 자산 재사용 / AC-03·06 | 완료: agent, registry, panel | 직접: 기존 자산 선택·버킷 생성·목록 확인 | 성공 경로 반복 안 함 |
| Record·개인 Skill·설정 / AC-05·06 | 완료: recorder, skills, settings | 직접: 시연·의도·다른 입력 검증·기본값 후속 실행 | 화면 02 확보 |
| Store / AC-07 | 완료: store API/UI | 직접: 게시 UI·HTTPS 설치. 사용자 전달: 동료 환경 두 번째 사용자 검증 완료 | 외부 대기 해제. 화면 03은 게시 상세·설치 진입 |
| 개선·적용·복구 / AC-08 | 구현 완료: improvement API/UI | 직접: 오류 주입 통제 데모의 실제 실패·Bedrock 후보·두 사례·Keeper 적용·채팅·복구 통과 | 자연 발생 개선으로 주장하지 않음. 화면 04 확보 |
| 오류·취소·사용자/탭 경계 / AC-09 | 구현 완료: coordinator, bridge, executor | 직접 실행의 실패/미확인 구분; 나머지 경계는 합성·TLS/WSS 검사 | 최종 통합 검토 완료. 상세 근거의 실제/합성 범위 유지 |
| 미등록 사이트 진입 / AC-10 | 구현 완료: prepare·관찰 후 전송 보류 | 직접: 로컬 Store를 같은 준비 경로로 관찰, 제한 문구 표시·Action observed·모델 0회. 사이트 필터 TLS 검사 | 모델 전송 지원 확대 없음 |
| 실행·제출 준비 | 명령·설정·README·CI 완료 | 직접 setup 재실행·종료/재시작·데이터 보존. 전체 61개 검사·타입·린트·빌드 통과 | 원격 CI 미실행. README에 기능별 촬영 준비 완료 |

검사 파일은 각 기능의 아래 기록에 연결한다. 이후 내용은 단계별 당시의 실행 이력이며 **현재 상태는 이 표가 기준**이다. 실제 모델 누계는 13회 / 보고 usage 49,352 tokens다. 최신 통제 데모가 추가한 3회는 개선 후보·개선 버전 채팅이며 모델 연결 검증·전체 자산 생성·동일 성공 실행을 반복하지 않았다.

## 기반·Repository

프로젝트 한정 Node 24.20.0 공식 배포 체크섬을 확인하고 npm 의존성을 정확한 버전과 lockfile로 설치했다. 전역 런타임은 변경하지 않았다. 공통 strict schema는 제한 동작·고정 실행 문맥·도구 묶음을 정의하며 Backend 전용 설정이 루트 .env를 읽는다. .env·런타임·SQLite·TLS·빌드 산출물은 Git 제외다.

SQLite는 WAL/FULL/외래 키와 버전 migration을 사용한다. Repository가 개인 소유 경계, 중복 접수, 불변 버전, 조건부 제어·설정 반영, 실제 결과 보존을 담당한다. 트랜잭션 내부 비동기 실행은 거절한다.

검사: `tests/backend/repository.test.ts` 7개 통과, `npm run typecheck` 통과. 사용자 격리·다른 입력의 중복 키·동일 생성의 대상 보존·취소/활성화·off/설정 충돌·실패/커밋 rollback·파일 DB 재열기·소유자 삭제 후 늦은 반영을 검사했다. 합성 데이터만 사용했다.

## 업무·API·Frontend

Coordinator가 사용자별 고정 대상/버전, 제어 revision, 변경 확인, 취소와 전달 순서를 조정한다. ModelScheduler는 생성/채팅 슬롯을 분리하고 소유자 순서를 순환한다. 예산 예약은 SQLite에 보존하며 응답 usage 미확인 시 예약을 유지한다. 미확인 동작은 대상 슬롯을 잡아두고 재전송 대신 기존 사후 조건의 읽기 전용 reconcile을 제공한다.

API는 해시된 접근 코드·만료 세션, Extension Origin·자격 인증, 256 KiB 상한, 소유 경계·제어 충돌을 검사한다. Backend는 HTTPS, 브리지는 인증된 WSS, Job별 내부 MCP는 공식 v2 SDK의 실제 Streamable HTTP를 사용한다. Bedrock은 Opus 설정을 읽고 SDK 자동 재시도를 끈다. 공유된 모델 게이트웨이가 승인된 합성 사이트 범위·입력 필터·Job 예산을 확인한다.

Extension은 사용자 툴바 동작에서 activeTab과 대상 탭을 고정한다. 패키지된 제한 해석기가 관찰/input/select/click/wait/navigation·사후 조건을 처리한다. 중복 Action 소비, 만료, 예상 이동과 임의 이동, Locator 없음/모호함, 민감 필드·frame/canvas 한계를 구분한다. Service Worker가 명령을 소유하고 페이지 메시지를 변경 확인으로 받지 않는다.

Keeper는 실제 API 상태로 자동 준비 진입, 채팅 초안 보존, 현재 대상, 작업/부분 준비/취소/미확인·변경 확인을 표시한다. Store 기본 화면은 실제 게시 목록을 조회한다. 개인 학습/설정·게시/설치·개선 화면은 후속 단계이며 완료로 표시하지 않는다.

검사: 8개 파일의 33개 테스트, typecheck/lint, 세 대상 빌드 통과. Repository 7, 업무 8, HTTPS/WSS 1, Extension 8, 생성/MCP 6, UI 2, 합성 범위 통합 1. MCP 검사는 실제 SDK 프로토콜이며 모델/브라우저 대역은 `tests/`의 명시된 합성 결과다. 새 사이트의 동일 준비 진입과 승인 범위 밖 모델 전송 차단을 검사했다.

## 진행 및 실제 증거

Step 1–10 구현·검사 완료. Step 11 진행 중. US-01~09/AC 전체 완료와 후속 단계 승인은 아직 아니다.

새 제품 로컬 TLS health를 WSL에서 인증서 검증과 함께 확인했다. Windows에서는 TrustFailure를 확인했고 사용자가 Chrome에 새 로컬 인증서를 신뢰 등록한 후 health 성공을 보고했다. Extension CORS credentials 응답과 툴바 기반 activeTab 취득을 수정하여 요청자의 기존 MinIO 탭과 실제 HTTPS 인증·WSS·관찰 연결을 확인했다. 다른 브라우저/관리자 세션이나 이전 probe를 대신 사용하지 않았다.

첫 새 제품 Opus 생성 요청: 모델 응답 수신, 요청 1회, 보고 usage 4,203 tokens, 브라우저 관찰 1회. 유효한 생성 후보 확정 전 중단되어 도구/Skill/실제 MCP 실행 성공은 아니다. 원시 응답은 로그/감사/Git에 저장하지 않았다. 구조화된 후보 반환 도구 스키마와 구분된 오류 처리를 보강해 후속 검증한다. 키 만료/잔여 크레딧은 미확인이다.

후속 실제 Opus 생성 2회(각각 5,213 / 5,420 tokens)에서 고정 후보를 생성하고 실제 MCP 호출이 기존 사용자 탭의 페이지 이동까지 이어졌다. 필수 라벨의 별표 처리와 문서 교체 타이밍 결함 때문에 업무 검증은 아직 실패다. 총 실제 모델 요청 3회/14,836 tokens이며 고정 후보를 다른 입력으로 재검증하는 경로를 추가해 실행기 수정 때 모델을 다시 부르지 않도록 했다. 완료된 변경은 자동 재실행하지 않고, 미확인 작업은 읽기 전용 상태 확인을 먼저 요구한다. 연결 성공과 업무 성공을 구분한다.

연결 수정 후 실제 Chrome에서 두 차례 재연결과 읽기 전용 사후 확인을 확인했다. 고정 후보 재검증은 모델 추가 호출 없이 실제 MCP를 통해 페이지 확인과 새 합성 값 입력까지 성공했다. 이어진 생성 버튼 클릭은 같은 텍스트의 제목/버튼 중복으로 ambiguous 판정되어 실행하지 않았다. 실제 클릭 가능한 요소만 해석하는 수정을 추가했고 중복 버튼 거절 회귀 검사를 포함해 27개 검사가 통과했다. 버킷 생성·기본 Skill 검증 완료로 기록하지 않는다. 입력한 합성 값은 현재 폼에 남아 있다.

### 첫 제품 전체 흐름 완료

현재 사용자가 선택한 demo Actor와 기존 로그인 탭에서 새 도구·기본 Skill을 각각 검증하고 같은 채팅에서 준비 자산을 재사용했다. 실제 생성된 합성 버킷은 `vibe-zoo-focus-tool-0909`, `vibe-zoo-final-skill-0909`, `vibe-zoo-chat-proof-0909` 세 개다. 채팅 실행은 실제 모델 2회·브라우저 동작 4회 뒤 사후 목록을 확인했다. 개인 Skill 학습·Store 설치·다른 브라우저 로그인 검증은 아직 아니다.

MinIO 입력의 focusin 수명과 비활성 버튼 검사를 수정했다. 최초 도구가 생성한 버킷은 남아 있었지만 후보의 결과 locator role이 실제 heading과 달랐다. 한 번의 제한된 모델 수정으로 불변 새 후보를 만들고 이미 실행된 사례를 읽기 전용으로 재평가했다. 기존 실패·unknown 영수증을 보존하며 생성 동작을 반복하지 않았다. 기본 Skill은 별도 새 입력으로 독립 검증했다. 수동 입력 진단은 제품 성공 증거에서 제외했다.

제품 실제 모델 호출 누계는 7회, 보고 usage 합계 25,731 tokens다. 상속된 Job 예산을 새 호출로 중복 계산하지 않았다. 기존 Haiku/Opus NFR probe와 별개이며 키 유효기간·잔여 크레딧은 미확인이다. 33개 회귀 검사는 unknown 보존·명시 미실행 검토·후보 검증 조건 약화 거절·독립 자산 검증·focusin/비활성 클릭을 포함한다.

[실제 생성·검증·채팅 화면](../../../../screenshots/01-generation-execution.png)은 Chrome 전체 화면에서 주소·자격 정보 없이 직접 캡처했다. 이미지 생성이나 결과 조작 없이 실제 MinIO 상태와 Keeper 응답을 보존했다.

### Record·개인 Skill 구현 진행

Step 11 코드: `src/extension/recorder.ts`, `src/extension/panel/learning.tsx`, `src/backend/generation/skills.ts`. 기존 관찰·제한 실행·MCP Validator와 Repository를 재사용한다. Record 동안 같은 대상의 자동 실행을 막고, Stop은 모델 호출 없이 의도 입력을 기다린다. 이벤트는 현재 Extension 메모리에만 두며 민감 필드/로그인 폼과 페이지가 합성한 이벤트를 제외한다. Stop 후 30분·취소·접수 확인 시 원시 버퍼를 버린다. 새 문서/다른 탭·출처로 넘어가 수집이 끊기면 재시연이 필요한 불완전 기록으로 처리한다.

명시적 의도와 최소 시연 근거로 초안을 한 번 생성하고 고정 도구 버전에 연결한다. 새 입력 보완은 같은 Job·후보·입력/제어 revision을 확인한다. 중복 접수는 기존 결과를 반환하며 완료한 검증은 재실행하지 않는다. 누락 원자 기능은 미준비 도구 후보/미지원 이유로 남긴다. 실제 개인 Skill 학습 검증은 아직 수행하지 않았으며 Step 11 체크박스를 열어둔다.

11개 파일 41개 테스트, typecheck/lint와 세 대상 build 통과. 새 합성 검사는 Record 슬롯·사용자/탭 경계, Stop에서 모델 미호출, 직접 의도 필요, 이벤트 필터/수명, 단일 컴파일·새 입력·revision/중복 보완과 미준비 후보, UI의 빈 의도 전송 금지를 확인했다. 원본 사례·성공 결과는 추가로 반복하지 않았다. 실제 학습 의도 답변을 기다리며 발표용 촬영/화면 다듬기는 전체 기능 완주 이후로 둔다.

Step 11 실제 완주: 기존 로그인 탭에서 Record 시작 후 `zoo-keeper-learn` 생성·Stop·의도 접수가 수행됐다. 직접 시연 변경은 학습 검증으로 재생하지 않았다. 실제 모델 1회/7,150 tokens로 개인 Skill 초안을 고정했고, 다른 입력 `vibe-zoo-personal-proof-0909`로 실제 MCP와 같은 대상 탭의 실행·사후 조건을 통과해 개인 자산을 활성화했다. 제품 모델 누계 8회/32,881 tokens. 원시 Record는 접수 확인 후 Extension 메모리에서 제거하며, 기존 도구/기본 Skill의 성공 검증을 반복하지 않았다.

Step 12 독립 코드와 합성 검사: Settings가 개인 이름/설명·typed 기본값·enabled를 CAS 저장한다. Registry는 off 자산/의존성을 신규 선택에서 제외하며 진행 중 스냅샷을 유지한다. MCP의 계약 검증 전에 해당 스냅샷의 기본값을 적용하고 명시 입력을 우선한다. 13개 파일 44개 테스트와 typecheck/lint 통과. 실제 기본값 후속 채팅은 진행 중이다.

Step 12 실제 완주: 개인 Skill의 이름을 ‘데모 버킷 만들기’로 저장하고 새 합성 버킷 이름을 개인 기본값으로 지정했다. 이름을 포함하지 않은 후속 채팅에서 해당 기본값으로 실제 개인 Skill이 실행되어 `vibe-zoo-default-proof-0909`를 생성하고 목록으로 확인했다. 모델 2회/4,866 tokens·브라우저 동작 4회, 제품 누계 10회/37,747 tokens. 코드 변경 전 성공 사례를 다시 검증한 것이 아니라 개인 기본값 기능의 새 입력 사례다. 실제 기록은 보존하고 후속 스크린샷 취합은 구현 우선 지시에 따라 Step 15로 모았다.

공유 경계 검토에서 변수 바인딩에 의해 덮어써지는 시연 인자가 Skill에 중복 저장된 것을 발견했다. 공개 투영과 모델 설명에는 이 사용되지 않는 인자를 제외하며, 실행의 ‘바인딩 우선’ 의미는 유지한다. 원본 불변 버전은 보존하고 공유 투영의 실제 인자 결과/개인 자료 제외를 합성 MCP 테스트로 확인한다.

### Step 13 — Store 게시·설치와 검증 출처

직접 수행: Keeper 공개 미리보기와 명시 게시로 개인 Skill/의존 도구 2개를 실제 공유했다. 개인 기본값·원본 시연 입력·로그인 필드의 공개 투영 제외를 확인했고 실제 HTTPS에서 demo-keeper 인증·catalog·개인 설치를 확인했다. 이 환경에서의 UI 설치 전체 흐름/설치자 브라우저 실행은 직접 수행하지 않았다.

사용자 전달 결과: Store의 두 번째 사용자 환경 검증을 동료 환경에서 완료했다고 보고받았다. 해당 외부 대기는 해제한다. 직접 확인한 API/게시 증거와 이 보고의 출처를 구분하며, 구체적 미제공 입력/실행 수치를 만들지 않는다. 현재 환경에서 별도 프로필 설치나 같은 실행을 반복하지 않는다.

### Step 14 — 개선 후보·회귀·명시 적용/복구 구현

`src/backend/generation/improvement.ts`와 `src/extension/panel/improvement.tsx`를 API/main/자산 화면에 연결했다. 현재 버전의 같은 소유자·실제 Action 근거가 있는 실패/성공 보고서를 선택하고, 기존 Opus Gateway로 이유가 있는 불변 후보를 만든다. 모델에는 최소 필터 관찰/결과와 기존 도구 참조만 전달한다. 입력 계약과 업무 사후 조건은 보존하며 기존 의존 도구를 재사용한다.

후보 검증은 원본/서로의 입력과 다른 두 입력으로 실제 Validator/MCP/대상 탭을 사용한다. 첫 사례 실패·미확인이면 후속 실행을 멈추며 중복 요청이 완료 동작을 재실행하지 않는다. 검증만으로 활성화하지 않고 특정 후보·보고서 digest·기준 버전·개인 revision에 대한 명시 적용을 요구한다. 포인터/적용 기록을 단일 SQLite 트랜잭션에서 저장하며 off·기본값·진행 중 스냅샷·기존 Publication을 보존한다. 복구는 검증된 이전 버전/의존성/현재 포인터를 확인한다. 적용 응답 유실 후 같은 요청을 보내도 복구를 되돌리지 않는다.

Agent의 실제 실행 결과도 버전별 `execution` 보고서로 보존한다. 실패/부분/미확인 뒤 추가 호출로 결과를 가리거나 암묵적으로 재시도하지 않는다. MCP에는 같은 이름의 개선 전후 고정 버전이 함께 존재할 수 있어 충돌 없는 호출 이름을 배정한다.

검증: 새 Backend 9개·UI 2개 검사 통과. 전체 58개 검사 통과 후 추가 중복 검증/재실행 방지 1개도 통과(총 59개). 타입 검사·린트·세 대상 빌드 통과. 로컬 DB를 비공개 백업 후 데이터 유지 재시작했고 실제 HTTPS health·인증된 개선 사례 조회 API를 확인했다. 추가 모델 호출/MinIO 변경은 없다.

증거 한계: 기본 Skill의 역사적 실패는 이미 Extension 입력 처리 수정으로 해결한 기록이다. 지금 재현되는 실패/새 모델 후보/실제 두 사례 통과/적용을 시연한 것으로 주장하지 않는다. Step 14 체크와 `04-improvement.png`는 실제 증거 확인 전까지 보류한다. Store 후속 결과는 위 Step 13의 사용자 전달 완료 보고를 따른다.

제품 Extension을 최신 빌드로 다시 로드하고 기존 demo-colleague MinIO 연결을 복원했다. 실제 Keeper 내 Skill 화면에서 개선·이전 버전 진입점이 표시되는 것을 확인했다. 모델 후보 생성/웹앱 변경은 실행하지 않았다.

### Step 15 — 실행·제출 준비와 최종 검토 보완

setup의 schema 2 재실행을 지원하고 기존 접근 코드/자산을 보존한다. `demo:stop`은 이 프로젝트 Backend만 정상 종료하며 `demo:cleanup`은 지정 소유자의 삭제 없는 미리보기가 기본이다. 실제 데모에서는 정리를 적용하지 않았다. 임시 DB의 삭제 검사만 수행했으며 실제 종료/재시작 전후 assets/versions/publications/installations의 내용이 동일함을 확인했다. TLS 인증서 검증을 유지한 health 확인도 통과했다.

README는 설치·필수 설정·키/계정·인증서·대표 사용법·제약/오류·실제 화면을 연결한다. CI는 시크릿 없는 검사/빌드/공개 파일 점검으로 구성했고 원격 실행은 아직 없다. `screenshots/02-personal-skill.png`, `03-store-install.png`는 실제 제품을 직접 캡처하여 육안 검토했다. 03은 설치 성공 화면으로 주장하지 않는다. 개선 화면/최종 촬영 시나리오는 실제 개선 검증 뒤 작성한다.

최종 검토에서 현재 사이트 외 자산이 준비 진입을 막는 문제를 수정했다. `/api/state`는 연결 사이트의 자산을 반환하고 허용 범위 밖 준비 요청은 실제 관찰 뒤 제한을 표시하며 모델로 보내지 않는다. 사이트 필터 TLS/API 검사와 합성 관찰/전송 차단 검사가 통과했다. Keeper Store 진입은 `chrome.tabs.create`를 명시해 대상 업무 탭 보존 의도를 코드로 보장한다. 관련 UI 검사 통과. 최신 Extension에서 실제 Store 새 탭이 열리고 기존 MinIO 탭들이 그대로 남는 것을 직접 확인했다.

전체 18개 파일 61개 테스트·typecheck·lint·Backend/Extension/Store 빌드가 통과했다. 테스트 추가 중 fixture 타입 오류를 수정한 뒤 최종 검사에서 해결을 확인했다. 최신 Backend 재시작과 제품 Extension 새로고침을 수행했다. 브라우저 복구 중 원래 일반 프로필의 MinIO 화면/로그인과 기존 버킷이 유지됨을 직접 확인했다. 다른 사용자 창 검증은 반복하지 않았다.

개선 검증의 최소 다음 경로: 현재 버전에서 정상 입력이 실패하는 화면 조건 하나를 확보하고 기존 성공 사례와 함께 기존 개선 기능을 사용한다. 현재 준비 자산은 버킷 생성뿐이며 알려진 실패는 이미 고쳐졌으므로 적합한 새 실패가 없다. 잘못된/중복 이름이나 로그인 만료를 개선 가능한 결함으로 꾸미지 않는다. 사용자에게 현재 미해결 사례 한 건을 질문했으며 응답 전에도 독립 작업을 완료했다. 사례가 없다면 실패를 재현할 조건을 정하는 일이 남으며 AC-08은 미검증 상태를 유지한다.

AC-10 실제 확인: 원래 MinIO 탭을 보존한 별도 Store 탭을 요청 대상에 연결했다. 같은 자동 prepare 경로에서 실제 관찰 Action 1개가 observed로 저장되고 모델 요청은 0회였다. Keeper 채팅에서 “현재 페이지를 관찰했습니다…도구 생성은 보류합니다. 관찰 제한: 표준 DOM 범위.”를 직접 확인했다. 합성 MinIO 밖 데이터를 모델로 보내거나 새 도구를 생성하지 않았다. 이는 Store 두 번째 사용자 설치 재검증이 아닌, 변경된 미등록 사이트 진입/관찰 경로의 검사다.

자연 발생 사례가 없을 때의 최소 검증 대안(미수행): 검증된 도구의 별도 데모 복사본에 낡은 locator 하나를 넣는 통제된 실패 주입이다. 원본 자산/설정/Store는 보존하고, 실제 브라우저에서 실패 및 관련 성공을 기록한 뒤 모델 후보·두 실제 사례 검증·명시 적용·새 입력 실행·복구를 확인한다. 이 경우 최초 결함은 인위적으로 만든 검증 조건임을 명시하고 실제 사용 중 발견한 개선 성과와 구분해야 한다. 현재 AC-08 통과 근거로 사용하지 않았다.

### Step 14 실제 검증 — 오류를 주입한 통제된 데모

사용자 승인에 따라 자연 발생 실패 탐색을 종료했다. 원본 검증 도구의 별도 복사본 `통제 데모 버킷 생성`을 만들고 대기 locator 하나가 데모 설정 입력 `legacyHeading`을 참조하도록 오류를 주입했다. 복사본은 의존 도구가 없으며 원본 Skill/Store와 연결하지 않았다. 초기 current/previous 포인터는 null이고 성공 보고서를 주입하지 않았다. 로컬 비공개 준비 스크립트는 실제 Backend의 기존 startup/workflow를 호출했으며 일반 제품 API/UI나 검증·활성화 규칙은 바꾸지 않았다.

| 실제 단계 | 입력 / 결과 |
|---|---|
| 오류 복사본 첫 실행 | `vibe-zoo-controlled-fail-0909`, 낡은 제목 `Create a Bucket`. 실제 제목 대기에서 실패, 입력/생성 전 중단. failed 보고서·미준비 유지 |
| 같은 버전의 관련 성공 확보 | `vibe-zoo-controlled-baseline-0909`, 실제 제목 `Create Bucket`. 실제 버킷 사후 확인 passed 후 일반 규칙으로 활성화 |
| 제품 개선 후보 | 실제 실패/성공 보고서로 Bedrock 1회·6,460 tokens. 모델이 대기 locator 값만 실제 관찰 제목 상수로 교체. 입력 계약·사후 조건·타임아웃·나머지 동작 동일 |
| 실패 재검증 | `vibe-zoo-controlled-improved-fail-0909`, 낡은 제목 유지. 실제 Action 4개·버킷 사후 조건 passed |
| 성공 회귀 | `vibe-zoo-controlled-regression-0909`, 정상 제목 유지. 실제 Action 4개·버킷 사후 조건 passed |
| Keeper 명시 적용 | 두 보고서 확인 후 실제 `검증한 후보 적용` 클릭. 검증 완료만으로는 바뀌지 않던 포인터가 후보로 변경됨 |
| Keeper 채팅 | `vibe-zoo-controlled-chat-0909`, 낡은 제목 유지. 개선 버전의 execution 보고서 passed·Action 4개·실제 버킷 확인. Bedrock 2회·5,145 tokens |

실행 출처: 최초 준비는 비공개 검증 스크립트, 관련 성공/후보/두 사례 접수는 실제 제품 HTTPS API, 각 웹앱 변경 확인·명시 적용·채팅 입력은 실제 Keeper UI다. 검증마다 실제 공식 MCP tools/list·tools/call과 요청 사용자 Extension/MinIO를 사용했다. 후보 작성과 정답 locator 교체는 모델이 수행했으며 사람이 후보/성공 조건을 수정하지 않았다. 단위 테스트 대역과 별개인 실제 실행 근거다. 최초 결함은 인위적으로 주입했으므로 자연 발생 문제 발견/해결 성과로 발표하지 않는다.

이번 모델 호출은 3회 / 보고 usage 11,605 tokens, 제품 누계 13회 / 49,352 tokens다. 기존 모델 연결 검사나 원본 전체 생성은 반복하지 않았다. 합성 버킷 4개가 새로 생성됐으며 기존 데이터는 삭제하지 않았다.


Keeper에서 `보존한 이전 버전으로 복구`를 클릭한 뒤 실제 current=오류 복사본 버전, previous=개선 후보로 변경됨을 확인했다. 같은 UI로 개선 버전을 다시 선택하여 촬영용으로 남겼다. 기본값/사용 여부와 실행 완료 기록을 보존했고 웹앱 변경을 되돌리지 않았다. 원본 assets/versions/publications/installations의 모든 기존 행이 준비 전과 동일함을 확인했다. `screenshots/04-improvement.png`는 실제 Chrome 화면에서 두 사례 통과·적용 기록·복구 진입과 새 버킷을 직접 캡처했다. 주소/프로필/자격정보가 없는 화면을 육안 검토했으며 이미지 편집/생성은 사용하지 않았다.

### 최종 Code Generation 검토

- US-01~09·AC-01~10 구현과 필수 대표 흐름 확인 완료. 직접 실행/사용자 전달 Store 검증/합성 경계 검사를 위 표와 상세 이력에서 구분한다. 사용자/탭·제어 revision·취소/미확인·불변 버전·실제 사후 상태 규칙을 완화하지 않았다.
- 최종 회귀/통합 검사: 18파일 61개 테스트(개선 Backend 9·UI 2 포함), 타입·린트, Backend/Extension/Store 빌드 통과. 핵심 TODO/미구현 표식 없음, diff 공백 검사 통과. 원격 GitHub CI 실행은 미확인이다.
- 실행·설정·종료·범위 제한 정리 명령, README·실제 화면 01~04·기능별 촬영 순서/초기 상태/입력/확인 결과 준비 완료. 새 촬영과 촬영용 빈 DB 생성은 아직 실행하지 않았다. 기존 데이터 초기화 없음.
- 일회성 준비 스크립트를 종료하고 표준 `npm run demo` Backend로 복귀했다. 추가 모델 연결 검사·새 환경 확대·관리 UI·일반화 기능은 만들지 않았다.
- 알려진 제한: 합성 MinIO 표준 DOM/Chrome 개발자 모드/현재 WSL 데모 범위, Whale 및 외부 참가자 접속 경로 미확정, 실제 키 만료·잔여 크레딧 미확인, 개선은 의도적 오류 주입 조건. Store 동료 환경 결과는 사용자 전달 출처다.
- Security/Resiliency/Property-Based Testing 확장은 기존 Enabled No / N/A. 기본 시크릿·입력·오류·사용자/탭 경계는 구현/검사에 포함한다. 코드 산출물 승인 후 정식 Build and Test 단계를 진행하며 그 승인을 선취하지 않는다.
